#
# Build/test extension binaries via DuckDB extension-ci-tools.
# Native desktop platforms (macOS / Linux / Windows). Wasm deferred — see ROADMAP.
#
name: Main Extension Distribution Pipeline
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' || github.sha }}
  cancel-in-progress: true

jobs:
  duckdb-build:
    name: Build extension binaries
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5-variegata
    with:
      # DuckDB *release* used to compile/test (not the C extension API stamp).
      # Makefile TARGET_DUCKDB_VERSION stays at v1.2.0 (stable C API metadata).
      duckdb_version: v1.5.5
      ci_tools_version: v1.5-variegata
      extension_name: healthkit_export
      extra_toolchains: python3
      # Health app export.zip is a portable file — native OS builds only.
      # Wasm excluded: large exports + bind-time buffering are a poor browser fit (ROADMAP).
      exclude_archs: "wasm_mvp;wasm_eh;wasm_threads"
